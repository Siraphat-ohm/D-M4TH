{
  "name": "d-m4th",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '@d-m4th/server' dev",
    "dev:web": "bun run --filter '@d-m4th/web' dev",
    "test": "bun test apps/server packages apps/web/src",
    "typecheck": "bun run --filter '*' typecheck",
    "search:ast": "ast-grep",
    "search:semgrep": "semgrep"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3"
  }
}
